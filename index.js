functions add (number) {
  return number += 3;
}

functions subtract (number) {
  return number -= 2;
}

functions multiply (number) {
  return number *= 10;
}

functions divide (number) {
  return number /= 5;
}
